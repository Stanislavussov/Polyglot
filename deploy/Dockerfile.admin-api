# ── Stage 1: base ─────────────────────────────────────────────────────
FROM node:26-alpine AS base
RUN npm install -g pnpm@10.14.0
WORKDIR /app

# ── Stage 2: deps ─────────────────────────────────────────────────────
FROM base AS deps

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY apps/bot/package.json apps/bot/
COPY apps/admin-api/package.json apps/admin-api/
COPY apps/admin/package.json apps/admin/
COPY apps/translation-benchmark/package.json apps/translation-benchmark/
COPY packages/admin-contracts/package.json packages/admin-contracts/
COPY packages/@types/package.json packages/@types/
COPY packages/core/package.json packages/core/
COPY packages/infra/package.json packages/infra/
COPY packages/adapters/ai/package.json packages/adapters/ai/
COPY packages/adapters/db/package.json packages/adapters/db/
COPY packages/adapters/notifications/package.json packages/adapters/notifications/
COPY packages/adapters/youtube/package.json packages/adapters/youtube/

RUN pnpm install --frozen-lockfile

# ── Stage 3: build ────────────────────────────────────────────────────
FROM deps AS build

COPY tsconfig.json ./
COPY apps/ apps/
COPY packages/ packages/

RUN pnpm --filter @polyglot/admin-api... run build

# ── Stage 4: production ──────────────────────────────────────────────
FROM base AS production

ENV NODE_ENV=production

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY apps/bot/package.json apps/bot/
COPY apps/admin-api/package.json apps/admin-api/
COPY apps/admin/package.json apps/admin/
COPY apps/translation-benchmark/package.json apps/translation-benchmark/
COPY packages/admin-contracts/package.json packages/admin-contracts/
COPY packages/@types/package.json packages/@types/
COPY packages/core/package.json packages/core/
COPY packages/infra/package.json packages/infra/
COPY packages/adapters/ai/package.json packages/adapters/ai/
COPY packages/adapters/db/package.json packages/adapters/db/
COPY packages/adapters/notifications/package.json packages/adapters/notifications/
COPY packages/adapters/youtube/package.json packages/adapters/youtube/

RUN pnpm install --frozen-lockfile --prod

COPY --from=build /app/apps/admin-api/dist apps/admin-api/dist
# admin-api routes import @polyglot/admin-contracts (workspace dep); ship its built dist for runtime resolution
COPY --from=build /app/packages/admin-contracts/dist packages/admin-contracts/dist
COPY --from=build /app/packages/core/dist packages/core/dist
COPY --from=build /app/packages/infra/dist packages/infra/dist
COPY --from=build /app/packages/adapters/db/dist packages/adapters/db/dist

USER node

EXPOSE 3001

HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD wget -qO- "http://127.0.0.1:${PORT:-3001}/healthz" || exit 1

CMD ["node", "apps/admin-api/dist/index.js"]
