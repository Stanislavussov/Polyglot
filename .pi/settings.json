{
  "$schema": "/Users/stanislav.ussov/Projects/ai-sdk/schema.json",
  "packages": ["/Users/stanislav.ussov/Projects/ai-sdk"],
  "orchestrator": {
    "agents": [
      {
        "name": "research",
        "role": "Evaluates and stress-tests ideas: business hypotheses, architecture decisions, tech stack choices. Sits between researcher and inventor — takes a raw idea, finds evidence for and against, identifies risks, trade-offs, and alternatives, then delivers a verdict with reasoning. Does NOT modify any files.",
        "rules": "Always present both sides — pros/cons, risks/opportunities.\nBack claims with evidence: benchmarks, case studies, ecosystem stats, known pitfalls.\nFor arch decisions — evaluate scalability, maintainability, team fit, migration cost.\nFor tech stack — compare at least 2-3 alternatives on concrete criteria.\nFor business ideas — assess market, feasibility, effort-to-value ratio.\nEnd with a clear verdict: recommend, reject, or pivot — never sit on the fence.\nRead-only — never modify, create, or delete files.",
        "model": "anthropic/claude-sonnet-4-6",
        "enabledTools": ["read", "bash"]
      },
      {
        "name": "i18n",
        "role": "Internationalization of all bot texts. Leaf agent — no dependencies on other modules. Works in packages/core/src.",
        "rules": "Only `t(key, lang)` — no direct locale file imports from other modules.\nOn missing key — fallback to `en`, never throw an error.\nKeys are a strict enum — TypeScript won't allow passing a non-existent key.\nInterpolation parameters are typed: `t(\"welcome\", lang, { name: string })`.\nSkills: t(key, lang, params?), getSupportedLangs(), isSupported(lang).\nTesting: after implementing, write Vitest tests next to the source file as `<module>.test.ts`. Use `import { describe, it, expect, vi } from 'vitest'`. Cover happy path, edge cases, error handling. Mock external deps. Use `.js` extensions in imports.",
        "type": "coding",
        "skills": [".pi/skills/i18n"]
      },
      {
        "name": "validation",
        "role": "Checks AI response quality. Leaf agent — no dependencies on other modules. Works in packages/core/src.",
        "rules": "Pure functions only — no side effects, no I/O.\nNever calls AI — only deterministic checks.\nEach rule is a separate function named `validate*`.\nAlways returns a failure reason — always explains what went wrong.\nSkills: validateSchema(raw, schema), validateSemantic(original, translation), validateLanguage(text, expectedLang), validateExamples(examples, word), validate(all above, orchestrated).\nTesting: after implementing, write Vitest tests next to the source file as `<module>.test.ts`. Use `import { describe, it, expect, vi } from 'vitest'`. Cover happy path, edge cases, error handling. Mock external deps. Use `.js` extensions in imports.",
        "type": "coding",
        "skills": [".pi/skills/validation"]
      },
      {
        "name": "db",
        "role": "The only module that knows about Drizzle and PostgreSQL. All other agents get data only through repositories. Works in packages/adapters/db/src.",
        "rules": "No business logic — CRUD operations only.\nAll queries typed via Drizzle — no raw SQL.\nSingle connection instance — singleton `getDb()`.\nEach repository is a separate file with a single responsibility.\nSkills: UserRepository (findByTelegramId, create, updateSettings), WordRepository (create, findByUser, findById, search, delete), TopicRepository (getCached, setCached, markInvalid).\nTesting: after implementing, write Vitest tests next to the source file as `<module>.test.ts`. Use `import { describe, it, expect, vi } from 'vitest'`. Cover happy path, edge cases, error handling. Mock Drizzle/pg — tests run without a real DB. Use `.js` extensions in imports.",
        "type": "coding",
        "skills": [".pi/skills/db"]
      },
      {
        "name": "ai",
        "role": "The only module that knows about OpenRouter and Vercel AI SDK. All other agents receive AI responses through it exclusively. Works in packages/adapters/ai/src.",
        "rules": "Has no knowledge of domain logic — only sends requests and returns responses.\nAll requests are logged: `model`, `tokens`, `cost_usd`, `duration_ms`.\nModel is always a parameter, never hardcoded internally.\n`maxRetries` is configurable from outside.\nSkills: generateObject<T>(prompt, schema, model), generateText(prompt, model), getAvailableModels(), estimateCost(tokens, model).\nTesting: after implementing, write Vitest tests next to the source file as `<module>.test.ts`. Use `import { describe, it, expect, vi } from 'vitest'`. Cover happy path, edge cases, error handling. Mock Vercel AI SDK calls — tests run without real API keys. Use `.js` extensions in imports.",
        "type": "coding",
        "dependsOn": ["validation"],
        "skills": [".pi/skills/ai"]
      },
      {
        "name": "translation",
        "role": "Translates words and phrases via AI. Knows about prompts and response structure. Works in packages/core/src.",
        "rules": "One method `translate()` — the single entry point.\nDoes not save results — only returns them.\nKnows nothing about the user — works only with text and languages.\nAlways calls the `validation` agent before returning a result.\nSkills: translate(input), translateBatch(words, ...), buildPrompt(input), parseResponse(raw).\nTesting: after implementing, write Vitest tests next to the source file as `<module>.test.ts`. Use `import { describe, it, expect, vi } from 'vitest'`. Cover happy path, edge cases, error handling. Mock AI adapter calls. Use `.js` extensions in imports.",
        "type": "coding",
        "dependsOn": ["ai", "validation"],
        "skills": [".pi/skills/translation"]
      },
      {
        "name": "topics",
        "role": "Manages topics and caching of dataset translations. Works in packages/core/src.",
        "rules": "Always checks cache before calling the `translation` agent.\nCalls `translation` in batch only — never one word at a time.\nKnows nothing about the user — works with language pairs.\nBuilt-in datasets are loaded once at startup.\nSkills: getBuiltinTopics(), getTopicWords(topicId, sourceLang, targetLangs), generateCustomTopic(prompt, sourceLang, targetLangs), getCacheStatus(topicId, sourceLang, targetLangs).\nTesting: after implementing, write Vitest tests next to the source file as `<module>.test.ts`. Use `import { describe, it, expect, vi } from 'vitest'`. Cover happy path, edge cases, error handling. Mock translation and DB deps. Use `.js` extensions in imports.",
        "type": "coding",
        "dependsOn": ["translation", "db"],
        "skills": [".pi/skills/topics"]
      },
      {
        "name": "notifications",
        "role": "Manages notification scheduling and delivery. Isolated from the bot via injection. Works in packages/adapters/notifications/src.",
        "rules": "Does not import the `bot` agent — receives `sendFn` as a parameter at startup.\nOne cron job for the entire schedule — do not create a job per user.\nOn send error — log and continue, do not stop the entire scheduler.\nRespect user timezone when sending.\nSkills: startScheduler(sendFn), stopScheduler(), getUsersForNotification(time), buildNotificationPayload(user), pickSuggestedWord(userId).\nTesting: after implementing, write Vitest tests next to the source file as `<module>.test.ts`. Use `import { describe, it, expect, vi } from 'vitest'`. Cover happy path, edge cases, error handling. Mock DB and cron deps. Use `.js` extensions in imports.",
        "type": "coding",
        "dependsOn": ["db", "topics"],
        "skills": [".pi/skills/notifications"]
      },
      {
        "name": "bot",
        "role": "Manages all user interaction via Telegram. The only module that knows about grammY. Works in apps/bot/src.",
        "rules": "Never contains business logic — only calls to other agents.\nNever accesses the DB directly — only through the `db` agent.\nAll texts only through the `i18n` agent — no hardcoded strings.\nEach scene is a separate file, max 100 lines.\nSkills: renderTranslation(output), renderTopicWord(word), handleOnboarding(), handleTranslate(), handleDictionary(), handleSettings().\nTesting: after implementing, write Vitest tests next to the source file as `<module>.test.ts`. Use `import { describe, it, expect, vi } from 'vitest'`. Cover happy path, edge cases, error handling. Mock grammY context and external deps. Use `.js` extensions in imports.",
        "type": "coding",
        "dependsOn": ["i18n", "db", "translation", "topics", "notifications"],
        "skills": [".pi/skills/bot"]
      },
      {
        "name": "test-runner",
        "role": "Runs the full test suite, analyzes failures, and fixes broken tests. This is the final quality gate in the pipeline.",
        "rules": "Run `pnpm test` from the project root first.\nIf all tests pass — report success summary with test count and coverage.\nIf tests fail — read the failing test files AND the source files they test.\nFix only test files — never modify source code.\nCommon fixes: update mocks to match changed signatures, fix import paths, adjust expected values.\nAfter fixing, re-run `pnpm test` to verify — repeat until green or max 3 attempts.\nIf tests still fail after 3 attempts — report the remaining failures clearly with file paths and error messages.\nNever skip or delete failing tests — fix them or report them.",
        "type": "coding",
        "dependsOn": ["bot"]
      }
    ]
  }
}
