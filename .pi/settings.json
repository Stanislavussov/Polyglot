{
  "$schema": "/Users/stanislav.ussov/Projects/ai-sdk/schema.json",
  "packages": [
    "/Users/stanislav.ussov/Projects/ai-sdk"
  ],
  "orchestrator": {
    "model": "anthropic/claude-opus-4-6",
    "agents": [
      {
        "name": "business",
        "standalone": true,
        "role": "Business analysis pipeline. Produces ONE SINGLE task file in docs/tasks/. All intermediate work is temporary.",
        "rules": "Pipeline produces exactly ONE artifact: a single comprehensive task file in docs/tasks/.\nAll intermediate agents write to .pi/tmp/business/ — these are temp working files, NOT permanent artifacts.\nNever modify docs/BRD.md, docs/roadmap.md, docs/mvp-scope.md, docs/requirements/, docs/tech-reqs/, docs/reviews/, docs/research/.\nThe final task-finalizer agent reads all temp artifacts and produces the ONE output file.\nBe concise — core information with nuances, no filler, no repetition. Tables over prose.",
        "model": "anthropic/claude-sonnet-4-6",
        "skills": [
          ".pi/skills/business"
        ],
        "enabledTools": [
          "read",
          "bash",
          "write"
        ],
        "subAgents": [
          {
            "name": "product",
            "role": "Competitor intelligence → .pi/tmp/business/research/competitors.md (TEMP)",
            "rules": "See skill for details. Score by impact × effort.\nALL output goes to .pi/tmp/business/research/ — NEVER write to docs/.\nBe dense — competitor table + short rationale per proposal, no essays.",
            "enabledTools": ["read", "bash", "write"],
            "skills": [".pi/skills/product"]
          },
          {
            "name": "researcher",
            "role": "Stress-tests hypotheses and tech decisions → .pi/tmp/business/research/evaluation.md (TEMP)",
            "rules": "See skill for details. Always end with a verdict.\nALL output goes to .pi/tmp/business/research/ — NEVER write to docs/.\nKeep evaluations tight — evidence bullets + risk table + verdict. No padding.",
            "enabledTools": ["read", "bash", "write"],
            "skills": [".pi/skills/research"]
          },
          {
            "name": "business-analyst",
            "role": "Synthesizes research into requirements → .pi/tmp/business/requirements/ (TEMP)",
            "rules": "See skill for details. Never prioritize — PO's job.\nALL output goes to .pi/tmp/business/ (e.g. .pi/tmp/business/brd.md, .pi/tmp/business/requirements/) — NEVER write to docs/.\nCompact requirements — ID, one-liner, acceptance criteria bullets. No narratives.",
            "enabledTools": ["read", "bash", "write"],
            "skills": [".pi/skills/business-analyst"],
            "dependsOn": ["product", "researcher"]
          },
          {
            "name": "product-owner",
            "role": "Prioritizes scope, defines MVP → .pi/tmp/business/scope/ (TEMP)",
            "rules": "See skill for details. MoSCoW method. Never modify BRD.\nALL output goes to .pi/tmp/business/scope/ (e.g. .pi/tmp/business/scope/roadmap.md, .pi/tmp/business/scope/mvp-scope.md) — NEVER write to docs/.\nRoadmap as tables, not paragraphs. One-line rationale per item.",
            "enabledTools": ["read", "bash", "write"],
            "skills": [".pi/skills/product-owner"],
            "dependsOn": ["business-analyst"]
          },
          {
            "name": "architect",
            "role": "Technical design: components, APIs, data models → .pi/tmp/business/tech-reqs/ (TEMP)",
            "rules": "See skill for details. Align with monorepo structure. DB-SOT for domain values.\nALL output goes to .pi/tmp/business/tech-reqs/ — NEVER write to docs/.\nDesign docs: boundaries + TS interfaces + decision table. No prose introductions.",
            "enabledTools": ["read", "bash", "write"],
            "skills": [".pi/skills/architect", ".pi/skills/db-sot"],
            "dependsOn": ["product-owner"]
          },
          {
            "name": "task-creator",
            "role": "Creates ONE comprehensive draft task → .pi/tmp/business/draft-task.md (TEMP)",
            "rules": "Read all upstream temp artifacts from .pi/tmp/business/.\nProduce ONE SINGLE comprehensive task file — NOT multiple tasks.\nThe task must be self-contained: goal, architecture overview, all subtasks/steps, types, acceptance criteria, edge cases, files to create/modify.\nWrite draft to .pi/tmp/business/draft-task.md — NEVER write to docs/.\nTask = title + goal + architecture + steps (with code examples) + acceptance criteria + edge cases + effort estimate.",
            "enabledTools": ["read", "bash", "write"],
            "skills": [".pi/skills/task-creator"],
            "dependsOn": ["architect"]
          },
          {
            "name": "brd-grooming",
            "role": "BRD vs draft task contradiction check → .pi/tmp/business/reviews/brd-grooming.md (TEMP)",
            "rules": "Compare docs/BRD.md against the draft task at .pi/tmp/business/draft-task.md.\nALL output goes to .pi/tmp/business/reviews/ — NEVER write to docs/.\nOne block per contradiction — quote + conflict + severity + fix. No preambles.\nDon't rewrite BRD or the draft task.",
            "enabledTools": ["read", "bash", "write"],
            "skills": [".pi/skills/brd-grooming"],
            "dependsOn": ["task-creator"]
          },
          {
            "name": "integrator",
            "role": "Cross-cutting consistency check → .pi/tmp/business/reviews/integration-review.md (TEMP)",
            "rules": "Review all temp artifacts in .pi/tmp/business/ plus live source code for consistency.\nALL output goes to .pi/tmp/business/reviews/ — NEVER write to docs/.\nFlag hardcoded language/mode lists — must be DB-driven.\nIssue table + traceability matrix. No summaries — findings speak for themselves.",
            "enabledTools": ["read", "bash", "write"],
            "skills": [".pi/skills/integrator", ".pi/skills/db-sot"],
            "dependsOn": ["task-creator"]
          },
          {
            "name": "task-finalizer",
            "role": "Merges draft task + review feedback into ONE FINAL task file → docs/tasks/",
            "rules": "Read the draft task from .pi/tmp/business/draft-task.md.\nRead review findings from .pi/tmp/business/reviews/ (brd-grooming.md, integration-review.md).\nIncorporate all Critical and Major review findings directly into the task — fix contradictions, add missing items, correct inconsistencies.\nMinor findings: incorporate if easy, otherwise note in an 'Open Questions' section.\nWrite exactly ONE file to docs/tasks/<NN>-<slug>.md where NN is the next available task number.\nThis is the ONLY file the entire business pipeline writes to docs/.\nAfter writing the final task, clean up: delete .pi/tmp/business/ directory.\nThe task must be fully self-contained — a developer must be able to implement it without reading any other document.",
            "enabledTools": ["read", "bash", "write"],
            "dependsOn": ["brd-grooming", "integrator"]
          }
        ]
      },
      {
        "name": "technical",
        "standalone": false,
        "role": "Technical implementation pipeline. Implements tasks from docs/tasks/ across all architecture layers with quality gates.",
        "rules": "Read task specs from docs/tasks/ before implementing.\nEach sub-agent owns its layer — no cross-boundary changes.\nAll agents write tests (Vitest) and update docs (SKILL.md + task files) — see dev-standards skill.\nFinal quality gates: test-runner then doc-validator.\nDB schema is the source of truth for all domain values — never hardcode languages, modes, or enums.",
        "skills": [
          ".pi/skills/technical"
        ],
        "subAgents": [
          {
            "name": "infra",
            "role": "Shared infrastructure: config loading, logger, and utility scripts. Works in packages/infra/src.",
            "rules": "No business logic — only cross-cutting infrastructure (config, logging, scripts).\nConfig via `loadConfig()` with Zod validation. Logger via shared pino instance.\nInfra is a leaf dependency — never import from domain packages at runtime.\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/dev-standards"
            ]
          },
          {
            "name": "translation-template",
            "role": "Translation output configuration: templates, presets, field visibility, and shared error classes. Works in packages/core/src/shared/.",
            "rules": "Shared utilities only — no adapter imports, no side effects.\n`DEFAULT_TEMPLATE` is the single source of truth for default field visibility.\nOutput presets are immutable constants — never mutate them.\n`resolveOutputConfig` is the only way to derive `TranslationOutputConfig` from a user template.\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/translation-template",
              ".pi/skills/dev-standards"
            ]
          },
          {
            "name": "i18n",
            "role": "Internationalization of all bot texts. Works in packages/core/src.",
            "rules": "Only `t(key, lang)` — no direct locale file imports from other modules.\nOn missing key — fallback to `en`, never throw.\nKeys are a strict TypeScript enum.\nLanguage lists from DB cache, not hardcoded arrays or union types.\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/i18n",
              ".pi/skills/db-sot",
              ".pi/skills/dev-standards"
            ]
          },
          {
            "name": "validation",
            "role": "Checks AI response quality with pure deterministic functions. Works in packages/core/src.",
            "rules": "Pure functions only — no side effects, no I/O, no AI calls.\nEach rule is a separate `validate*` function with failure reason.\nLanguage validation via DB language-cache, not hardcoded ISO maps.\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/validation",
              ".pi/skills/db-sot",
              ".pi/skills/dev-standards"
            ],
            "dependsOn": [
              "i18n"
            ]
          },
          {
            "name": "db",
            "role": "The only module that knows Drizzle/PostgreSQL. All other agents get data through repositories. Works in packages/adapters/db/src.",
            "rules": "No business logic — CRUD only. All queries via Drizzle — no raw SQL.\nSingleton `getDb()`. Each repository in a separate file.\nThis module is the ONLY source of truth for languages, modes, and domain constants. Export named constants for schema defaults (DEFAULT_ACTIVE_MODE, DEFAULT_TIMEZONE, MAX_LEARNING_LANGS).\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/db",
              ".pi/skills/db-sot",
              ".pi/skills/dev-standards"
            ],
            "dependsOn": [
              "validation",
              "i18n"
            ]
          },
          {
            "name": "ai",
            "role": "The only module that knows OpenRouter/Vercel AI SDK. Works in packages/adapters/ai/src.",
            "rules": "No domain logic — only sends requests and returns responses.\nAll requests logged: model, tokens, cost_usd, duration_ms.\nModel is always a parameter, never hardcoded. maxRetries configurable.\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/ai",
              ".pi/skills/dev-standards"
            ],
            "dependsOn": [
              "infra",
              "validation"
            ]
          },
          {
            "name": "translation",
            "role": "Translates words/phrases via AI. Knows prompts and response structure. Works in packages/core/src.",
            "rules": "Single entry point: `translate()`. Does not save results.\nKnows nothing about the user — text and languages only.\nAlways validates before returning.\nLanguage names in prompts from DB cache (`getLangName()`), not hardcoded maps.\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/translation",
              ".pi/skills/context-enrichment",
              ".pi/skills/db-sot",
              ".pi/skills/dev-standards"
            ],
            "dependsOn": [
              "ai",
              "validation"
            ]
          },
          {
            "name": "topics",
            "role": "Manages topics and caching of dataset translations. Works in packages/core/src.",
            "rules": "Cache-first — always check before calling translation.\nBatch-only translation calls. Knows nothing about the user.\nLanguage codes from callers (DB settings), never hardcoded.\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/topics",
              ".pi/skills/db-sot",
              ".pi/skills/dev-standards"
            ],
            "dependsOn": [
              "translation",
              "db"
            ]
          },
          {
            "name": "dictionary-pipeline",
            "role": "Config-driven pipeline for reading words from a user's dictionary. Works in packages/core/src/modules/dictionary-pipeline/.",
            "rules": "Pure core module — no adapter imports, all deps injected via `DictionaryPipelineDeps`.\nPreset configs are the single source of truth for word selection strategies.\n`TemplateFields` controls field visibility — loaded from user's saved template.\nNever hardcode language codes or user settings.\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/dictionary-pipeline",
              ".pi/skills/dev-standards"
            ],
            "dependsOn": [
              "translation",
              "translation-template"
            ]
          },
          {
            "name": "notifications",
            "role": "Notification scheduling and delivery, isolated from bot via injection. Works in packages/adapters/notifications/src.",
            "rules": "Receives `sendFn` — never imports bot. One cron job for all users.\nLog and continue on send errors. Respect user timezone.\nTimezone and language defaults from DB constants, not hardcoded.\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/notifications",
              ".pi/skills/db-sot",
              ".pi/skills/dev-standards"
            ],
            "dependsOn": [
              "infra",
              "db",
              "topics"
            ]
          },
          {
            "name": "bot",
            "role": "All user interaction via Telegram (grammY). Works in apps/bot/src.",
            "rules": "No business logic — only calls other agents.\nNo direct DB access — only through db agent.\nAll texts via i18n — no hardcoded strings.\nModes and languages from DB constants and cache, not hardcoded unions or string literals.\nEach scene in a separate file, max 100 lines.\nAfter implementing: write Vitest tests, update SKILL.md and task files.",
            "type": "coding",
            "skills": [
              ".pi/skills/bot",
              ".pi/skills/db-sot",
              ".pi/skills/dev-standards"
            ],
            "dependsOn": [
              "infra",
              "i18n",
              "db",
              "translation",
              "translation-template",
              "dictionary-pipeline",
              "topics",
              "notifications"
            ]
          },
          {
            "name": "deps-validator",
            "role": "Runs dependency-cruiser to validate module import direction. Ensures no circular or forbidden imports exist across the codebase. This is a pre-test quality gate in the pipeline.",
            "rules": "Run `pnpm lint:deps` from the project root.\nIf exit code is 0 — report success with a summary of packages/modules checked.\nIf exit code is non-zero — read the violation output carefully.\nFor each violation: identify the source file, the forbidden import, and which rule it breaks.\nAttempt to fix violations by restructuring imports (extract shared types, use dependency injection, break cycles).\nAfter fixing, re-run `pnpm lint:deps` to verify — repeat until clean or max 3 attempts.\nIf violations remain after 3 attempts — report them clearly with file paths, rule names, and suggested fixes.\nNever disable or weaken dependency-cruiser rules — fix the code, not the config.",
            "type": "coding",
            "dependsOn": [
              "infra",
              "bot",
              "notifications",
              "topics",
              "dictionary-pipeline",
              "translation-template",
              "translation",
              "ai",
              "validation",
              "db",
              "i18n"
            ]
          },
          {
            "name": "test-runner",
            "role": "Runs full test suite, analyzes failures, fixes broken tests. Final quality gate.",
            "rules": "Run `pnpm test` from project root first. Fix only test files — never source code.\nRe-run after fixes — max 3 attempts. Never skip or delete failing tests.\nUse DB-exported constants in mocks, not hardcoded string literals.",
            "type": "coding",
            "skills": [
              ".pi/skills/test-runner",
              ".pi/skills/db-sot"
            ],
            "dependsOn": [
              "deps-validator",
              "infra",
              "bot",
              "notifications",
              "topics",
              "dictionary-pipeline",
              "translation-template",
              "translation",
              "ai",
              "validation",
              "db",
              "i18n"
            ]
          },
          {
            "name": "doc-validator",
            "role": "Validates all documentation against current code state. Final pipeline gate.",
            "rules": "Verify SKILL.md files: Current State, File Structure, Public API, Types, Schema.\nVerify task files: checkboxes, file lists, README status. Move ✅ Done tasks to docs/tasks/finished/.\nFlag hardcoded language/mode unions in docs — must reference DB cache.\nFix docs only — never source code.",
            "type": "coding",
            "skills": [
              ".pi/skills/doc-validator",
              ".pi/skills/db-sot"
            ],
            "dependsOn": [
              "test-runner"
            ]
          }
        ]
      }
    ]
  }
}
