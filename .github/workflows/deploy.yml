name: Deploy to VPS

on:
  push:
    branches: [master]
  workflow_dispatch:

env:
  REGISTRY: docker.io

permissions:
  contents: read
  packages: write

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
    secrets:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}

  push:
    needs: ci
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Refresh admin report artifacts
        run: pnpm reports:sync-admin

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile
          target: production
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/polyglot-bot:${{ github.sha }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile.admin-api
          target: production
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/polyglot-admin-api:${{ github.sha }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile.admin
          target: production
          build-args: |
            PUBLIC_API_URL=https://${{ secrets.ADMIN_API_DOMAIN }}
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/polyglot-admin:${{ github.sha }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile.landing
          target: production
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/polyglot-landing:${{ github.sha }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile
          target: migrate
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/polyglot:migrate-${{ github.sha }}

  deploy:
    needs: push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Prepare files
        run: |
          cp deploy/docker-compose.yml ./docker-compose.yml

          RELEASE_ANNOUNCEMENT_BASE64="$(python - <<'PY'
          from pathlib import Path
          import base64

          text = Path("CHANGELOG.md").read_text(encoding="utf-8")
          lines = text.splitlines()
          in_unreleased = False
          collected = []
          for line in lines:
              if line.startswith("## [Unreleased]"):
                  in_unreleased = True
                  continue
              if in_unreleased and line.startswith("## ["):
                  break
              if in_unreleased:
                  collected.append(line)

          message = "\n".join(collected).strip()
          if message:
              print(base64.b64encode(message.encode("utf-8")).decode("ascii"))
          PY
          )"

          echo "BOT_IMAGE_NAME=${{ secrets.DOCKER_USERNAME }}/polyglot-bot:${{ github.sha }}" >> .env
          echo "MIGRATE_IMAGE_NAME=${{ secrets.DOCKER_USERNAME }}/polyglot:migrate-${{ github.sha }}" >> .env
          echo "ADMIN_API_IMAGE_NAME=${{ secrets.DOCKER_USERNAME }}/polyglot-admin-api:${{ github.sha }}" >> .env
          echo "ADMIN_IMAGE_NAME=${{ secrets.DOCKER_USERNAME }}/polyglot-admin:${{ github.sha }}" >> .env
          echo "LANDING_IMAGE_NAME=${{ secrets.DOCKER_USERNAME }}/polyglot-landing:${{ github.sha }}" >> .env
          echo "BOT_TOKEN=${{ secrets.BOT_TOKEN }}" >> .env
          echo "OPENROUTER_API_KEY=${{ secrets.OPENROUTER_API_KEY }}" >> .env
          echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" >> .env
          echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> .env
          echo "ADMIN_PANEL_URL=https://${{ secrets.ADMIN_PANEL_DOMAIN }}" >> .env
          echo "PUBLIC_API_URL=https://${{ secrets.ADMIN_API_DOMAIN }}" >> .env
          echo "REPOSITORY=${{ secrets.REPOSITORY }}" >> .env
          echo "RELEASE_ID=${{ github.sha }}" >> .env
          echo "RELEASE_AUDIENCE_GROUPS=admin,tester" >> .env
          echo "RELEASE_ANNOUNCEMENT_BASE64=${RELEASE_ANNOUNCEMENT_BASE64}" >> .env

      - name: Copy Compose and Env to VPS
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "docker-compose.yml,.env"
          target: "/opt/polyglot"
          overwrite: true

      - name: Deploy and Restart on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /opt/polyglot

            echo "${{ secrets.DOCKER_TOKEN }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

            docker compose pull

            docker network create polyglot-network 2>/dev/null || true

            docker compose --profile tools run --rm migrate

            docker compose run --rm admin-api node apps/admin-api/dist/seed.js

            docker compose up -d --remove-orphans

            if grep -q '^RELEASE_ANNOUNCEMENT_BASE64=.' .env; then
              docker compose run --rm --no-deps bot node apps/bot/dist/release-announcement.cli.js
            else
              echo "no announcement content"
            fi

            docker image prune -af
            docker builder prune -af 2>/dev/null || true
